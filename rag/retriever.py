def retrieve_context(product_name):
    # Simulated retrieval logic
    context = f"""
    Product: {product_name}
    Pricing rules: Avoid pricing above 1000 CZK unless bundled.
    Market trend: Price sensitivity increases above 950 CZK.
    """
    return context
